# Trading Agent — Python dependencies
# Install: pip install -r requirements.txt

ccxt>=4.0.0        # Exchange connectivity — free public data, no API key needed
pandas>=2.0.0      # Data manipulation and OHLCV handling
